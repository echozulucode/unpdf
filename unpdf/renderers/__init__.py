"""Renderers for unpdf.

This package contains modules for rendering processed content:
    - markdown: Markdown output generation
    - html: HTML output generation (future)

Example:
    from unpdf.renderers.markdown import MarkdownRenderer

    renderer = MarkdownRenderer()
    markdown = renderer.render(elements)
    print(markdown)
"""
